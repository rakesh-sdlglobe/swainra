import 'package:flutter/material.dart';

class AppConstants {
  static const defaultDuration = Duration(milliseconds: 300);
  static const defaultCurve = Curves.easeInOut;

  static const Duration defaultAnimDuration = Duration(milliseconds: 400);
  static const Curve easeInOut = Curves.easeInOut;
  static const Curve fastOutSlowIn = Curves.fastOutSlowIn;

  static const double defaultPadding = 16.0;
  static const double defaultRadius = 12.0;

  static const shimmerBaseColor = Color(0xFFE0E0E0);
  static const shimmerHighlightColor = Color(0xFFF5F5F5);
}
